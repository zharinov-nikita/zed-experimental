# 03 — Модель состояния подвала и блокировка действий

Спецификация: `.scratch/voice-dictation-3/spec.md`. Словарь: `CONTEXT.md`.

**What to build:** Prefactor плюс поведение. Подвал Dictation Window рисуется по результату чистой функции «фаза и признаки идущих задач → доступные действия, их подписи, спиннер, метки слева». Пока идут «Recognizing…» и Post-processing, доступен только Cancel; Accept, Tab и Resume погашены и не срабатывают ни кликом, ни с клавиатуры; в подвале спиннер с подписью. Поведение «принять, когда закончится» удалено: Accept работает только когда есть что принимать. Функция состояния становится швом для тикетов 04, 05 и 07.

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] Тесты функции состояния по каждой фазе: Starting, Recording, Recognizing, Post-processing, Review с обработанным текстом и без, Failed; для каждой перечислены включённые и выключенные действия.
- [ ] Во время Post-processing Enter и Tab ничего не делают, Escape отменяет; клики по погашенным подсказкам не срабатывают.
- [ ] Спиннер и «Post-processing…» / «Recognizing…» видны в подвале, исчезают по завершении.
- [ ] Логика «accept when done» удалена, тесты `agent_ui` зелёные.
- [ ] `./script/clippy` зелёный.
